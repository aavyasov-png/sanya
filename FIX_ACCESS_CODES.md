# Работа с кодами доступа (bcrypt + API)

## Архитектура

Система использует безопасное хранение кодов с bcrypt-хешированием:

- **Frontend (App.tsx)**: Отправляет запросы к API endpoints
- **API (/api/auth/verify-code)**: Проверяет коды через bcrypt
- **API (/api/admin/access-codes)**: Создаёт/удаляет коды (требует авторизации)
- **Database (Supabase)**: Хранит хеши кодов, НЕ сами коды

## Структура таблицы access_codes

```sql
access_codes:
  - id: UUID PRIMARY KEY
  - code_hash: TEXT (bcrypt hash кода, UNIQUE)
  - role_to_assign: TEXT (viewer/editor/admin/owner)
  - max_uses: INTEGER (лимит использований, NULL = ∞)
  - uses_count: INTEGER (счётчик использований)
  - expires_at: TIMESTAMP (дата истечения)
  - is_disabled: BOOLEAN (отключён ли код)
  - created_by: UUID (кто создал)
  - created_at: TIMESTAMP (когда создан)
  - note: TEXT (заметка администратора)
```

## Применение миграции

1. Выполните миграцию в Supabase SQL Editor:
```sql
-- Содержимое из migrations/001_users_and_roles.sql
```

2. Опционально добавьте поле display_code для UI:
```sql
-- Содержимое из create_simple_access_codes.sql
```

## Использование

### Создание кода (Админ)

1. Откройте админ-панель (код SANYA4565)
2. Перейдите в "Коды доступа"
3. Выберите:
   - Роль (viewer/editor/admin/owner)
   - Макс. использований (0 = неограниченно)
   - Срок истечения (опционально)
   - Заметку (опционально)
4. Нажмите "🔐 Создать код доступа"
5. **ВАЖНО**: Код показывается ОДИН РАЗ! Сразу скопируйте его.

### Проверка кода (Пользователь)

1. Откройте приложение
2. Введите полученный код
3. Примите правила
4. Система:
   - Хеширует введённый код
   - Сравнивает с хешами в БД через bcrypt
   - Проверяет срок действия и лимиты
   - Создаёт сессию с JWT токеном
   - Присваивает роль

## Безопасность

✅ **Что защищено:**
- Коды хранятся как bcrypt-хеши (необратимое шифрование)
- Сравнение через bcrypt.compare() на сервере
- Исходный код показывается только при создании
- API защищены JWT авторизацией
- Rate limiting на проверку кодов

❌ **Что НЕ хранится:**
- Исходные коды в базе данных
- Коды в логах
- Коды в localStorage (только роль и токен)

## API Endpoints

### POST /api/auth/verify-code
Проверка кода доступа (публичный endpoint)

**Request:**
```json
{
  "code": "ABC123XYZ456",
  "telegram_id": 123456789,  // optional
  "full_name": "Имя"         // optional
}
```

**Response (success):**
```json
{
  "success": true,
  "token": "jwt_token_here",
  "user": {
    "id": "uuid",
    "full_name": "Имя",
    "role": "viewer",
    "telegram_id": 123456789
  }
}
```

### GET /api/admin/access-codes
Получить список кодов (метаданные, без самих кодов)

**Headers:**
```
Authorization: Bearer <jwt_token>
```

**Response:**
```json
{
  "codes": [
    {
      "id": "uuid",
      "role_to_assign": "viewer",
      "max_uses": null,
      "uses_count": 3,
      "expires_at": "2024-12-31T23:59:59Z",
      "is_disabled": false,
      "created_at": "2024-01-01T00:00:00Z",
      "note": "Тестовый код"
    }
  ]
}
```

### POST /api/admin/access-codes
Создать новый код доступа

**Headers:**
```
Authorization: Bearer <jwt_token>
```

**Request:**
```json
{
  "role_to_assign": "viewer",
  "max_uses": null,
  "expires_at": "2024-12-31T23:59:59Z",
  "note": "Описание"
}
```

**Response:**
```json
{
  "code": "ABC123XYZ456",  // !!! Показывается ТОЛЬКО здесь
  "id": "uuid",
  "role_to_assign": "viewer",
  "expires_at": "2024-12-31T23:59:59Z",
  "max_uses": null
}
```

### DELETE /api/admin/access-codes?id=<uuid>
Отключить код (устанавливает is_disabled=true)

**Headers:**
```
Authorization: Bearer <jwt_token>
```

## Отладка

Включите логирование в браузере (DevTools Console):

```javascript
// При проверке кода:
[CODE] Checking code: ABC123...
[CODE] API response status: 200
[CODE] API success: { success: true, token: "...", user: {...} }
[CODE] Code valid, granting access with role: viewer

// В localStorage:
access_ok = "1"
user_role = "viewer"
session_token = "jwt_token_here"
```

## Troubleshooting

**Проблема**: Код не принимается
- Проверьте, что таблица создана (migrations/001_users_and_roles.sql)
- Убедитесь, что код не истёк (expires_at)
- Проверьте is_disabled = false
- Проверьте uses_count < max_uses (если max_uses не NULL)

**Проблема**: Ошибка при создании кода
- Проверьте JWT токен в localStorage (session_token)
- Убедитесь, что роль пользователя = admin или owner
- Проверьте логи Cloudflare Functions

**Проблема**: Список кодов пустой
- Создайте хотя бы один код через админ-панель
- Проверьте RLS политики в Supabase
- Проверьте авторизацию (JWT токен)
