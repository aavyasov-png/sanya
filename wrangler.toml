# This project uses Cloudflare Pages
# Deploy via Cloudflare Dashboard or CLI: wrangler pages deploy dist

name = "sanya"
compatibility_date = "2026-01-30"

# Static assets directory (built by Vite)
[assets]
directory = "./dist"

# Environment variables will be set in Cloudflare dashboard:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
# - VITE_TELEGRAM_BOT_TOKEN
# - VITE_AI_PROVIDER
# - VITE_GROQ_API_KEY
