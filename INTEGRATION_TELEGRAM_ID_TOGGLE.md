# 🔧 Управление привязкой к Telegram ID

## Описание

По умолчанию интеграция с Uzum требует наличия Telegram ID пользователя для работы. Теперь эту проверку можно отключить с помощью флага в коде.

## Расположение флага

**Файл:** [src/App.tsx](src/App.tsx#L527-L531)

```typescript
// 🔧 ФЛАГ: Требовать Telegram ID для интеграций
// Установите false для работы без привязки к Telegram ID
const REQUIRE_TELEGRAM_ID = false;
```

## Режимы работы

### ✅ REQUIRE_TELEGRAM_ID = true (требуется Telegram ID)
- Интеграция работает **только** для пользователей Telegram
- При отсутствии Telegram ID операции будут прерываться
- Каждый пользователь получает изолированную интеграцию
- **Безопасно для продакшена**

### ⚠️ REQUIRE_TELEGRAM_ID = false (Telegram ID не требуется)
- Интеграция работает **без** привязки к Telegram
- Если Telegram ID нет, используется `default_user`
- Подходит для тестирования и разработки
- **Не рекомендуется для продакшена** (все пользователи будут использовать одну интеграцию)

## Как переключить режим

### Включить проверку Telegram ID (продакшен)
```typescript
const REQUIRE_TELEGRAM_ID = true;
```

### Выключить проверку Telegram ID (разработка/тестирование)
```typescript
const REQUIRE_TELEGRAM_ID = false;
```

## Затронутые функции

При изменении флага автоматически изменяется поведение:

1. **`loadUzumIntegration()`** - загрузка интеграции из БД
2. **`handleSaveToken()`** - сохранение токена в БД
3. **`handleDisconnect()`** - отключение интеграции

## Технические детали

### Когда флаг = true
```typescript
const userId = getTelegramUserId();
if (!userId && REQUIRE_TELEGRAM_ID) {
  // Остановить выполнение
  return;
}
```

### Когда флаг = false
```typescript
const userId = getTelegramUserId();
const actualUserId = userId || 'default_user'; // Используем fallback
// Продолжить с actualUserId
```

## Рекомендации

### Для локальной разработки
```typescript
const REQUIRE_TELEGRAM_ID = false; // Легче тестировать
```

### Для продакшена
```typescript
const REQUIRE_TELEGRAM_ID = true; // Изоляция пользователей
```

## Безопасность

⚠️ **ВАЖНО:** При `REQUIRE_TELEGRAM_ID = false` все пользователи без Telegram ID будут использовать один и тот же `user_id = 'default_user'` в базе данных.

Это означает:
- Общий токен для всех
- Нет изоляции данных
- Подходит только для тестирования

## История изменений

- **2026-01-29**: Добавлен флаг `REQUIRE_TELEGRAM_ID` для управления привязкой
- Исходная реализация требовала Telegram ID всегда

## Связанные файлы

- [src/App.tsx](src/App.tsx) - основной файл с флагом
- [migrations/002_integrations.sql](migrations/002_integrations.sql) - схема таблицы интеграций
- [UZUM_ARCHITECTURE.md](UZUM_ARCHITECTURE.md) - архитектура интеграции
