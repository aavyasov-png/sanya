# 🏗️ АРХИТЕКТУРА АДМИН-ПАНЕЛИ (Диаграмма)

## ТЕКУЩАЯ СТРУКТУРА

```
┌─────────────────────────────────────────────────────────────────┐
│                      TELEGRAM MINI APP                          │
│                    (React 18 + TypeScript)                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │
                  ┌───────────┴───────────┐
                  │                       │
            ┌─────▼─────┐          ┌─────▼──────┐
            │  HOME PAGE │          │  ADMIN TAB │
            │            │          │            │
            └─────┬─────┘          └─────┬──────┘
                  │                      │
                  │                      ├─── 📂 Sections
                  │                      ├─── 🗂️  Cards
                  │                      ├─── 📰 News
                  │                      ├─── ❓ FAQ
                  │                      ├─── 🔑 Access Codes
                  │                      └─── 🚀 Crawl
                  │
                  └────────────────┬───────────────┘
                                   │
                    ┌──────────────▼──────────────┐
                    │    API CLIENT (api.ts)      │
                    │  + localStorage tokens      │
                    └──────────────┬──────────────┘
                                   │
                    ┌──────────────▼──────────────┐
                    │      SUPABASE (DB)          │
                    │    PostgreSQL Tables        │
                    └─────────────────────────────┘
```

## СУЩЕСТВУЮЩИЕ ТАБЛИЦЫ

```
┌──────────────────────────────────────────────────────────────────────┐
│                        DATABASE LAYER                                │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ✅ sections      ─── разделы меню (Sections)                       │
│     ├─ id, key, title_ru, title_uz, icon, sort, etc                │
│     └─ RBAC: EDITOR+ can write, all can read                        │
│                                                                       │
│  ✅ cards         ─── карточки в разделах (Cards)                   │
│     ├─ id, section_id, title_ru, title_uz, body_ru/uz, etc         │
│     └─ RBAC: EDITOR+ can write, all can read                        │
│                                                                       │
│  ✅ news          ─── новости (News)                                │
│     ├─ id, title_ru, title_uz, body_ru/uz, published_at, pinned    │
│     └─ RBAC: ADMIN+ can write, all can read                         │
│                                                                       │
│  ✅ faq           ─── часто задаваемые вопросы (FAQ)               │
│     ├─ id, question_ru, question_uz, answer_ru/uz, sort            │
│     ├─ ❌ ОТСУТСТВУЕТ: slug, category (нужно для ШАГ 6)            │
│     └─ RBAC: ADMIN+ can write, all can read                         │
│                                                                       │
│  ✅ access_codes  ─── коды доступа для регистрации                │
│     ├─ id, code_hash, role, max_uses, uses_count, expires_at       │
│     ├─ ⚠️  Валидация max_uses/expires_at отсутствует               │
│     └─ RBAC: ADMIN+ can write, all can read metadata only          │
│                                                                       │
│  ✅ users         ─── пользователи системы                          │
│     ├─ id, telegram_id, email, full_name, role, is_active          │
│     ├─ ❌ Компонент существует, не интегрирован в админку!         │
│     └─ RBAC: OWNER can write, ADMIN+ can read                       │
│                                                                       │
│  ⚠️ user_sessions ─── активные JWT сессии                          │
│     ├─ id, user_id, token_hash, expires_at, ip_address             │
│     └─ Таблица создана, но не используется (localStorage mode)     │
│                                                                       │
│  ❌ audit_log     ─── история всех действий (НЕ ЛОГИРУЕТСЯ!)      │
│     ├─ id, user_id, action, resource_type, resource_id, details    │
│     └─ Таблица создана, но никто не пишет в неё!                   │
│                                                                       │
│  ❌ microcopy     ─── управляемые текстовые фрагменты (НЕ СОЗДАНА) │
│     ├─ id, key, text_ru, text_uz (для ШАГ 7)                      │
│     └─ НУЖНО СОЗДАТЬ для управления micro-texts                    │
│                                                                       │
│  ❌ settings      ─── конфигурация приложения (НЕ СОЗДАНА)         │
│     ├─ id, key, value_ru, value_uz, value_type                     │
│     └─ НУЖНО СОЗДАТЬ для управления настройками                    │
│                                                                       │
│  ⚠️ integrations  ─── зашифрованные токены Uzum/Kaspi              │
│     └─ Работает, но нет управления в админке                       │
│                                                                       │
│  ⚠️ manual_sections ─── краулер документации                        │
│     └─ Создана, но не интегрирована в админку                      │
│                                                                       │
│  ⚠️ telegram_subscribers ─── подписчики Telegram бота              │
│     └─ Используется для уведомлений, но нет управления             │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
```

## ТЕКУЩИЕ ADMIN ROUTES

```
/admin (route.name === "admin")
│
├─── adminTab === "sections"       (App.tsx lines ~4450)
│    │   ✅ Form: key, icon, title_ru, title_uz, sort
│    │   ✅ List: все разделы с кнопками удаления
│    │   ✅ RBAC: canEdit() проверяет [OWNER, ADMIN, EDITOR]
│    │   ❌ Нет логирования изменений
│    │
│    └─── adminSaveSection() ──────▶ supabase.from('sections').insert/update
│         adminDeleteSection() ────▶ supabase.from('sections').delete
│
├─── adminTab === "cards"         (App.tsx lines ~4500)
│    │   ✅ Form: section_id, title_ru/uz, body_ru/uz, sort, file_url, map_url
│    │   ✅ List: все карточки с редактированием/удалением
│    │   ✅ RBAC: canEdit()
│    │   ❌ Нет логирования изменений
│    │
│    └─── adminSaveCard() ────────▶ supabase.from('cards').insert/update
│         adminDeleteCard() ──────▶ supabase.from('cards').delete
│
├─── adminTab === "news"          (App.tsx lines ~4650)
│    │   ✅ Form: title_ru/uz, body_ru/uz, published_at, pinned, image_url
│    │   ✅ List: все новости с редактированием/удалением
│    │   ✅ RBAC: canManage()
│    │   ✅ Отправка уведомлений в Telegram
│    │   ❌ Нет логирования изменений
│    │
│    ├─── adminSaveNews() ────────▶ supabase.from('news').insert/update
│    ├─── adminDeleteNews() ──────▶ supabase.from('news').delete
│    └─── sendTelegramNotification() ──▶ Telegram Bot API
│
├─── adminTab === "faq"           (App.tsx lines ~4750)
│    │   ✅ Form: question_ru/uz, answer_ru/uz, sort
│    │   ✅ List: все вопросы с редактированием/удалением
│    │   ✅ RBAC: canManage()
│    │   ❌ Отсутствует поле slug для ContextualFaqLink (ШАГ 6)
│    │   ❌ Отсутствует поле category для группировки
│    │   ❌ Нет логирования изменений
│    │
│    └─── adminSaveFaq() ────────▶ supabase.from('faq').insert/update
│         adminDeleteFaq() ──────▶ supabase.from('faq').delete
│
├─── adminTab === "codes"         (App.tsx lines ~4860)
│    │   ✅ Form: role, max_uses, expires_at, note
│    │   ✅ List: коды с деактивацией
│    │   ✅ RBAC: canManage()
│    │   ⚠️  Валидация max_uses при использовании (server-side отсутствует)
│    │   ⚠️  Валидация expires_at при использовании (server-side отсутствует)
│    │   ❌ Нет логирования попыток использования кодов
│    │
│    └─── <AccessCodesManagement /> ──▶ отдельный компонент (227 строк)
│         ├─── loadCodes() ────────▶ api.getAccessCodes()
│         ├─── createCode() ───────▶ api.createAccessCode()
│         └─── deleteCode() ───────▶ api.deleteAccessCode()
│
└─── ОТСУТСТВУЮЩИЕ TABS:
     ❌ adminTab === "users"        ──── UsersManagement.tsx есть, не интегрирован!
     ❌ adminTab === "audit"        ──── audit_log есть, но нет UI для просмотра
     ❌ adminTab === "settings"     ──── settings не создана
     ❌ adminTab === "microcopy"    ──── microcopy не создана
```

## RBAC МАТРИЦА (текущая)

```
┌─────────────────────────────────────────────────────────────────┐
│                    ROLE PERMISSIONS MATRIX                      │
├──────────────┬────────┬───────┬────────┬─────────┬──────────────┤
│ Operation    │ VIEWER │ EDITOR│ ADMIN  │ OWNER   │ Current Code │
├──────────────┼────────┼───────┼────────┼─────────┼──────────────┤
│ Read All     │   ✓    │   ✓   │   ✓    │    ✓    │  canRead()   │
│ Edit Section │   ✗    │   ✓   │   ✓    │    ✓    │  canEdit()   │
│ Edit Card    │   ✗    │   ✓   │   ✓    │    ✓    │  canEdit()   │
│ Create News  │   ✗    │   ✗   │   ✓    │    ✓    │ canManage()  │
│ Create FAQ   │   ✗    │   ✗   │   ✓    │    ✓    │ canManage()  │
│ Create Code  │   ✗    │   ✗   │   ✓    │    ✓    │ canManage()  │
│ Manage Users │   ✗    │   ✗   │   ✗    │    ✓    │canFullAccess│
│ View Audit   │   ✗    │   ✗   │   ✓    │    ✓    │   ❌ N/A     │
│ Run Crawl    │   ✗    │   ✗   │   ✗    │    ✓    │canFullAccess│
└──────────────┴────────┴───────┴────────┴─────────┴──────────────┘

⚠️  ПРОБЛЕМЫ:
   1. Роли хранятся в localStorage (может быть подделана!)
   2. Нет server-side проверок (все доверяет клиенту)
   3. Нет JWT токена (нельзя валидировать роль)
   4. RLS политики все разрешают (USING (true))
```

## FLOW ДИАГРАММА: Использование кода доступа

```
ПОЛЬЗОВАТЕЛЬ
│
└─► Вводит код в input
    │
    └─► hashCode(entered) ─────────────┐
                                        │
                                        ▼
    SUPABASE QUERY
    ├─ WHERE code_hash = hash
    ├─ AND is_active = true
    ├─ AND (expires_at IS NULL OR expires_at > now()) ❌ ВАЛИДАЦИЯ ОТСУТСТВУЕТ
    ├─ AND (max_uses IS NULL OR uses_count < max_uses) ❌ ВАЛИДАЦИЯ ОТСУТСТВУЕТ
    └─ SINGLE
       │
       ├─ NO MATCH ──▶ showToast("Неверный код")
       │
       └─ FOUND ──────▶ ✅ Берём role из базы
                        │
                        ├─► localStorage.setItem("user_role", role)
                        ├─► localStorage.setItem("admin_ok", "1") if ADMIN+
                        ├─► setRoute({ name: "home" })
                        │
                        └─► ❌ НЕ ЛОГИРУЕМ использование в audit_log!
                             ❌ НЕ УВЕЛИЧИВАЕМ uses_count!

ПРОБЛЕМЫ:
  1. uses_count никогда не обновляется (несовместимо с max_uses!)
  2. expires_at не проверяется на сервере (может использовать просрочку)
  3. Нет логирования попыток ввода
  4. Нет rate limiting (можно перебирать коды)
```

## КОМПОНЕНТЫ (их статус)

```
✅ РАБОТАЮТ И ИНТЕГРИРОВАНЫ:
   ├─ EmptyState.tsx          (104 строк)  - пустые состояния
   ├─ ContextualTooltip.tsx   (174 строк)  - контекстные подсказки (ШАГ 4)
   ├─ ContextualFaqLink.tsx   (47 строк)   - контекстные FAQ links (ШАГ 6)
   ├─ UzumStatusBlock.tsx     - статус Uzum (ШАГ 1)
   ├─ GettingStartedBlock.tsx - быстрый старт (ШАГ 1-2)
   ├─ UzumDashboard.tsx       - dashboard с empty state (ШАГ 3)
   ├─ UzumProducts.tsx        - товары с empty state (ШАГ 3)
   ├─ UzumOrders.tsx          - заказы с empty state (ШАГ 3)
   ├─ UzumFinance.tsx         - финансы с empty state (ШАГ 3)
   └─ AccessCodesManagement.tsx (227 строк) - управление кодами

❌ СУЩЕСТВУЮТ, НО НЕ ИНТЕГРИРОВАНЫ:
   └─ UsersManagement.tsx     (не добавлен в admin routes!)

❌ НУЖНО СОЗДАТЬ:
   ├─ AuditLogViewer.tsx      - просмотр истории действий
   ├─ SettingsEditor.tsx      - управление конфигурацией
   ├─ MicrocopyManager.tsx    - управление micro-texts
   └─ FAQ Category Manager    - управление категориями FAQ
```

---

## ИТОГИ ИНВЕНТАРИЗАЦИИ

```
✅ ГОТОВО (для ШАГ 1-7):
   ├─ Основной UI компоненты  (Empty States, Tooltips, FAQ Links)
   ├─ Основные таблицы БД     (sections, cards, news, faq, users)
   ├─ Система кодов доступа   (access_codes таблица + компонент)
   └─ RBAC система            (4 роли: viewer, editor, admin, owner)

⚠️  ТРЕБУЕТ УЛУЧШЕНИЙ:
   ├─ FAQ: добавить slug/category (для ШАГ 6)
   ├─ Microcopy: создать таблицу (для ШАГ 7)
   ├─ Access Codes: улучшить валидацию
   ├─ RBAC: перевести на JWT tokens
   └─ Audit Logging: включить логирование

❌ НЕ ГОТОВО (в production):
   ├─ Users Management интеграция
   ├─ Audit Log просмотр
   ├─ Settings управление
   ├─ Security (localStorage RBAC)
   ├─ Rate limiting на кодах
   └─ Полнотекстовый поиск по контенту
```
