# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ: Ошибка деплоя на Cloudflare Pages

## Проблема
```
✘ [ERROR] Missing entry-point to Worker script or to assets directory
```

## Причина
В настройках проекта Cloudflare Pages указана команда деплоя, которая не нужна для статических сайтов с Functions.

## ✅ РЕШЕНИЕ (2 минуты)

### Шаг 1: Откройте настройки проекта
1. Перейдите на https://dash.cloudflare.com
2. Выберите ваш проект Pages
3. Откройте **Settings** → **Builds & deployments**

### Шаг 2: Измените настройки

Найдите секцию **Build configurations** и установите:

```
Framework preset: None
Build command: npm run build
Build output directory: dist
Root directory: (оставьте пустым или /)
```

### Шаг 3: ВАЖНО! Удалите команду деплоя

Найдите поле **Deploy command** и попробуйте один из вариантов:

#### Вариант 1: Оставьте пустым (если возможно)
- Удалите любую команду деплоя
- Оставьте поле ПУСТЫМ

#### Вариант 2: Если поле требует значение
Укажите безопасную команду, которая ничего не делает:
```
echo "Static site deployed automatically"
```

или просто:
```
:
```

#### Вариант 3: Удалите все поле
Если есть кнопка "Remove" или крестик рядом с полем - нажмите её

Также удалите **Version command** если оно есть (тем же способом).

### Шаг 4: Сохраните и переделайте деплой

1. Нажмите **Save**
2. Перейдите в **Deployments**
3. Нажмите **Retry deployment** на последнем деплое

## Почему это работает?

Cloudflare Pages **автоматически** загружает файлы из директории `dist` после успешной сборки и поддерживает Functions из папки `/api` без дополнительных команд.

## Добавление переменных окружения

После исправления настроек:

1. Перейдите в **Settings** → **Environment variables**
2. Добавьте переменные:
   ```
   VITE_SUPABASE_URL=https://ykbouygdeqrohizeqlmc.supabase.co
   VITE_SUPABASE_ANON_KEY=<ваш ключ из .env>
   ```
3. Сохраните и пересоберите проект

Теперь всё должно работать! ✅
