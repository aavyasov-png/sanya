# 🔧 ПОЛНАЯ ИНСТРУКЦИЯ НАСТРОЙКИ

## ⚠️ Проблема
Если у вас не работают:
- 📸 Фото в новостях
- 📋 Правила пользования
- 👤 Имя пользователя
- 📨 Уведомления в Telegram

То нужно выполнить следующие шаги.

---

## 1️⃣ НАСТРОЙКА БАЗЫ ДАННЫХ

### Шаг 1: Добавить колонку image_url

1. Откройте [console.supabase.com](https://console.supabase.com)
2. Выберите ваш проект
3. Левое меню → **SQL Editor**
4. Нажмите **New Query**
5. Вставьте этот код:

```sql
ALTER TABLE news ADD COLUMN IF NOT EXISTS image_url TEXT;
```

6. Нажмите кнопку **RUN** (зелёная кнопка с треугольником)

✅ Колонка добавлена! Теперь в новостях будет поле для изображений.

---

## 2️⃣ НАСТРОЙКА TELEGRAM БОТА

### Шаг 1: Создать бота

1. Откройте Telegram
2. Найдите **@BotFather**
3. Напишите команду: `/newbot`
4. Следуйте инструкциям:
   - **Bot name**: Выберите имя (например "My News Bot")
   - **Bot username**: Выберите уникальное имя (например "my_news_123_bot")
5. **Скопируйте Bot Token** (выглядит так: `123456789:ABCDefGHIjklMNOPqrstUvwxYZ`)

⚠️ **Сохраните этот токен!**

### Шаг 2: Создать канал

1. Создайте новый приватный канал
2. Дайте ему имя (например "News Updates")
3. Сделайте его закрытым (Only members can see this channel)

### Шаг 3: Добавить бота в канал

1. Откройте ваш новый канал
2. Нажмите на имя канала сверху
3. **Add Members** (или "Добавить участников")
4. Найдите вашего бота (используя username из шага 1)
5. Добавьте его как **администратора**

### Шаг 4: Получить Chat ID

Есть два способа:

#### Способ А: Через Web API (проще)

1. Откройте в браузере:
```
https://api.telegram.org/bot{BOT_TOKEN}/getUpdates
```

**Замените `{BOT_TOKEN}` на ваш токен!** Например:
```
https://api.telegram.org/bot123456789:ABCDefGHIjklMNOPqrstUvwxYZ/getUpdates
```

2. Откройте эту ссылку в браузере
3. Найдите в JSON: `"chat":{"id":-1001234567890}`
4. Скопируйте этот ID (включая минус!)

#### Способ Б: Через сообщение в канал

1. Отправьте любое сообщение в ваш канал
2. Используйте бота для проверки:
```
https://api.telegram.org/bot{BOT_TOKEN}/getUpdates
```

---

## 3️⃣ ДОБАВЛЕНИЕ ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

### Локально (.env файл)

1. Откройте файл `/workspaces/vite-react2.0/.env`
2. Если его нет - создайте его в корне проекта
3. Добавьте:

```bash
VITE_TELEGRAM_BOT_TOKEN=123456789:ABCDefGHIjklMNOPqrstUvwxYZ
VITE_TELEGRAM_CHAT_ID=-1001234567890
```

**Замените на ваши значения!**

Пример правильного формата:
```bash
VITE_TELEGRAM_BOT_TOKEN=987654321:XYZabcDEFghiJKLmnoPQRstUvwxyz123456
VITE_TELEGRAM_CHAT_ID=-1009876543210
```

### На Vercel (продакшн)

1. Откройте [vercel.com](https://vercel.com)
2. Выберите ваш проект
3. **Settings** → **Environment Variables**
4. Добавьте две переменные:

| Key | Value |
|-----|-------|
| VITE_TELEGRAM_BOT_TOKEN | 123456789:ABCDefGHIjklMNOPqrstUvwxYZ |
| VITE_TELEGRAM_CHAT_ID | -1001234567890 |

5. Нажмите **Add**
6. Нажмите **Deploy** (или перейдите на Deployments и нажмите Redeploy на последний деплой)

---

## 4️⃣ ПРОВЕРКА

### Локально

1. Останавливайте текущий dev сервер (Ctrl+C)
2. Запустите заново:
```bash
npm run dev
```

3. Откройте консоль браузера (F12)
4. Перейдите в ваше приложение
5. Ищите логи:
   - `[TG]` - информация про Telegram
   - `[TELEGRAM]` - информация про уведомления

### На Vercel

1. Перейдите на https://vite-react20.vercel.app (или ваш URL)
2. Откройте консоль (F12)
3. Ищите те же логи
4. Если нет логов - значит переменные не подхватились
5. Перезагрузите страницу несколько раз

---

## 5️⃣ ТЕСТИРОВАНИЕ

### Тест 1: Имя пользователя

1. Откройте приложение в Telegram Mini App
2. Посмотрите внизу - должно быть ваше имя из Telegram
3. Если пусто:
   - Откройте консоль (F12)
   - Ищите `[TG] Setting user:`
   - Если там пусто - проблема в Telegram WebApp API

### Тест 2: Правила пользования

1. На экране приветствия должна быть кнопка **"📋 Правила пользования"**
2. Нажмите на неё - должен раскрыться список
3. Если нет кнопки - проверьте `src/App.tsx` строка ~695

### Тест 3: Фото в новостях

1. Перейдите в админ панель (код: SANYA4565)
2. Вкладка **"Новости"**
3. Добавьте новость с URL картинки (полная ссылка!)
4. Пример URL: `https://images.unsplash.com/photo-1234567890abcdef`
5. Нажмите **"Добавить"**
6. Посмотрите в списке новостей - должна быть картинка

Если картинка не появилась:
- Проверьте, что URL начинается с `https://`
- Проверьте, что URL верный (откройте его в браузере)
- Проверьте, что CORS включены на сервере изображений
- Откройте консоль - ищите ошибки загрузки изображения

### Тест 4: Уведомления в Telegram

1. Добавьте новость в админ панели с фото и текстом
2. Нажмите **"Добавить"**
3. Откройте консоль (F12)
4. Ищите логи:
   - `[TELEGRAM] ✓ Уведомление отправлено` - успех!
   - `[TELEGRAM] ✗ Ошибка отправки:` - ошибка

Если ошибка:
- **"Bot token или chat ID не установлены"** → добавьте переменные в .env
- **"fetch failed"** → проверьте интернет
- **"Unauthorized"** → Bot Token неверный
- **"Bad Request"** → Chat ID неверный

5. Откройте свой Telegram канал - должно быть уведомление

---

## ❌ ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ

### Ошибка 1: "Фото не добавляются"
**Причины:**
- ❌ Колонка `image_url` не добавлена в БД
- ❌ URL картинки неверный (не начинается с https://)
- ❌ CORS не включены на сервере картинок

**Решение:**
1. Выполните SQL: `ALTER TABLE news ADD COLUMN IF NOT EXISTS image_url TEXT;`
2. Используйте полные URL с https://
3. Проверьте URL в браузере - должна открываться картинка

### Ошибка 2: "Имя не подтягивается"
**Причины:**
- ❌ Приложение не открыто в Telegram (открыто просто в браузере)
- ❌ WebApp API не инициализирован
- ❌ Telegram версия старая

**Решение:**
1. Откройте приложение через Telegram (Mini App)
2. Откройте консоль (F12 в Telegram Desktop или браузер DevTools)
3. Ищите логи `[TG] Setting user:`
4. Если логов нет - проверьте main.tsx строка 1-10

### Ошибка 3: "Правила не показываются"
**Причины:**
- ❌ Code был отредактирован и потерял функцию
- ❌ State `rulesExpanded` не инициализирован

**Решение:**
1. Проверьте строку ~225 в App.tsx: `const [rulesExpanded, setRulesExpanded] = useState(false);`
2. Проверьте строку ~695: есть ли кнопка с `onClick={() => setRulesExpanded(!rulesExpanded)}`
3. Если нет - восстановите из git: `git checkout src/App.tsx`

### Ошибка 4: "Уведомления не отправляются"
**Причины:**
- ❌ Переменные не установлены
- ❌ Bot Token или Chat ID неверный
- ❌ Бот не админ в канале
- ❌ Канал приватный (может быть нужно иное разрешение)

**Решение:**
1. Проверьте консоль: `[TELEGRAM] Bot token или chat ID не установлены`
2. Если видна эта ошибка - добавьте переменные в .env
3. Перезагрузите dev сервер: `npm run dev`
4. Проверьте, что бот имеет права администратора в канале
5. Попробуйте отправить сообщение боту напрямую через API

---

## 📞 ЕСЛИ ВСЕ ЕЩЕ НЕ РАБОТАЕТ

1. **Откройте консоль браузера (F12)**
2. **Скопируйте все логи, начинающиеся с [TG], [TELEGRAM], [CODE], [DATA], [SUPABASE]**
3. **Проверьте:**
   - Есть ли `[TG] initDataUnsafe:` логи?
   - Есть ли в них данные пользователя?
   - Какие ошибки в [TELEGRAM] логах?

4. **На Vercel** проверьте Build Logs:
   - Settings → Build & Development Settings
   - Посмотрите последний build
   - Ищите ошибки

---

## ✅ ЧЕКЛИСТ ПЕРЕД ПУБЛИКАЦИЕЙ

- [ ] SQL миграция выполнена: `ALTER TABLE news ADD COLUMN IF NOT EXISTS image_url TEXT;`
- [ ] Bot Token добавлен в `.env` и на Vercel
- [ ] Chat ID добавлен в `.env` и на Vercel
- [ ] Бот добавлен администратором в Telegram канал
- [ ] Vercel redeploy выполнен (Settings → Deployments → Redeploy)
- [ ] Тестовая новость добавлена
- [ ] Картинка отображается в новостях
- [ ] Уведомление пришло в Telegram канал
- [ ] Имя пользователя отображается внизу
- [ ] Правила пользования раскрываются при нажатии на кнопку

