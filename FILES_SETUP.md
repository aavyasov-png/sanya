# 📥 ЗАГРУЗКА И СКАЧИВАНИЕ ФАЙЛОВ

## ✅ ЧТО ДОБАВЛЕНО

### Админ панель (Карточки)
- ✅ Новое поле: **"URL файла (опционально)"**
- ✅ Можно указать URL на файл при создании карточки
- ✅ Файл НЕ обязателен - поле опциональное

### Для пользователей
- ✅ Если файл прикреплен → видна кнопка **"📥 Скачать файл"**
- ✅ Красивая кнопка с наведением (hover эффект)
- ✅ Клик → скачивание файла

---

## 🚀 ЧТО НУЖНО СДЕЛАТЬ

### Шаг 1: SQL Миграция (ОБЯЗАТЕЛЬНО!)
Откройте Supabase SQL Editor и выполните:

```sql
ALTER TABLE cards ADD COLUMN IF NOT EXISTS file_url TEXT;
```

Без этого поле не будет сохраняться в БД!

### Шаг 2: На Vercel
1. Верхнее меню → **Deployments**
2. Найдите последний деплой
3. Нажмите → **Redeploy**
4. Дождитесь **Ready** ✓

---

## 📝 КАК ИСПОЛЬЗОВАТЬ

### В админ панели:
1. Вкладка **"Карточки"**
2. Заполните все данные (заголовок, текст и т.д.)
3. **NEW:** В новом поле добавьте URL файла:
   - Пример: `https://example.com/document.pdf`
   - Пример: `https://example.com/guide.docx`
4. Нажмите **"Добавить"**

### Для пользователей:
1. Откройте карточку
2. Если файл прикреплен → видна кнопка **"📥 Скачать файл"**
3. Нажмите → файл скачается

---

## 💡 ПРИМЕРЫ URL ФАЙЛОВ

```
✅ https://example.com/file.pdf
✅ https://example.com/document.docx
✅ https://example.com/image.zip
✅ https://cdn.example.com/data.xlsx

❌ Локальные пути (/home/user/file.pdf)
❌ Google Drive ссылки (требуют обхода блокировок)
❌ Ссылки требующие авторизации
```

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

- **Тип:** `file_url?: string` - опциональное поле
- **Хранение:** Supabase таблица `cards`
- **Скачивание:** HTML `<a href download>` - безопасный способ
- **UI:** Кнопка появляется только если `file_url` не пусто

---

## 📚 ЧТО БЫЛО ИЗМЕНЕНО

| Файл | Изменение |
|------|-----------|
| `src/App.tsx` | Добавлено поле `file_url` в тип CardRow, input в админ панели, кнопка скачивания для пользователя |
| `.env` | Telegram переменные (уже добавлены) |
| БД | Нужно выполнить SQL: `ALTER TABLE cards ADD COLUMN IF NOT EXISTS file_url TEXT;` |

